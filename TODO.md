# TODO - Convert PHP backend to Node.js

- [x] Inspect existing PHP backend (`api/contact.php`) and frontend (`index.html`) behavior
- [x] Create Node.js Express backend (`node-backend/`) with MySQL insert + validation
- [x] Update frontend `index.html` to call Node endpoint (`http://localhost:3000/api/contact`)
- [x] Install Node dependencies in `node-backend/`
- [x] Start Node server
- [x] Test contact form end-to-end (success + failure cases)

- [ ] (Optional) Remove or deprecate old PHP endpoint (`api/contact.php`) references

